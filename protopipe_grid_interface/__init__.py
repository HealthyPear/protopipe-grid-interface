from .version import __version__

__all__ = ["__version__"]

# log = logging.getLogger(__name__)
# log.setLevel(logging.DEBUG)  # ensures that everything will be logged

# formatter = logging.Formatter("%(asctime)s:%(levelname)s:%(message)s")

# fh = logging.FileHandler(f"upload_{args.model_type}.log", mode="w", encoding="utf-8")
# fh.setLevel(getattr(logging, logging.DEBUG))
# fh.setFormatter(formatter)
# log.addHandler(fh)

# ch = logging.StreamHandler()
# ch.setLevel(getattr(logging, logging.INFO))
# ch.setFormatter(formatter)
# log.addHandler(ch)
